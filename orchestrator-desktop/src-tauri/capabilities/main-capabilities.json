{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capabilities",
  "description": "Capabilities for the main application",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
